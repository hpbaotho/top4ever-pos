using System;

namespace Top4ever.ClientService.Enum
{
    public enum RET_VALUE
    {
        SUCCEEDED = 1,
        ERROR_COMM = 2,
        ERROR_DB = 3,
        ERROR_AUTHENTICATION = 4
    }
}
